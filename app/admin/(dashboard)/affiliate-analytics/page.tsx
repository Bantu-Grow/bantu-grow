import { getProgramAnalytics } from '@/lib/affiliate'
const money=new Intl.NumberFormat('id-ID',{style:'currency',currency:'IDR',maximumFractionDigits:0})
export default async function AffiliateAnalyticsPage(){const data=await getProgramAnalytics();const metrics=[['Affiliate aktif',data.activeAffiliates],['Klik referral',data.clicks],['Lead teratribusi',data.attributedLeads],['Pelanggan',data.customers],['Pendapatan lunas',money.format(data.paidRevenue)],['Komisi ditahan',money.format(data.heldCommission)],['Komisi tersedia',money.format(data.availableCommission)],['Komisi dibayar',money.format(data.paidCommission)]];const conversion=data.clicks?data.customers/data.clicks*100:0;return <div className="space-y-6"><div><h2 className="text-2xl font-extrabold">Analytics Affiliate</h2><p className="text-sm text-muted-foreground">Ringkasan funnel, pendapatan, dan kewajiban komisi program.</p></div><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{metrics.map(([label,value])=><div key={label} className="rounded-xl border bg-card p-5"><p className="text-sm text-muted-foreground">{label}</p><p className="mt-2 text-2xl font-bold">{value}</p></div>)}</div><div className="rounded-xl border bg-card p-6"><h3 className="font-bold">Konversi klik ke pelanggan</h3><p className="mt-2 text-3xl font-extrabold text-primary">{conversion.toFixed(1)}%</p></div></div>}